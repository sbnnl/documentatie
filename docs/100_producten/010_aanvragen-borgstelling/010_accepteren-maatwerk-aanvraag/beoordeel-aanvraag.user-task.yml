user-task:
  name: Beoordeel aanvraag
  schema:
    $ref: "../../../data-dictionary/user_tasks/beoordeel_aanvraag.yml"
  ui:
    hidden:
      - taak_id
      - borgstelling_id
actions:
  goedkeuren:
    name: Goedkeuren
    schema: 
      $ref: "../../../data-dictionary/commands/keur_aanvraag_goed.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id
        - actie
  afwijzen:
    name: Afwijzen
    schema: 
      $ref: "../../../data-dictionary/commands/wijs_aanvraag_af.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id
        - actie
  vraag_stellen:
    name: Vraag stellen
    schema: 
      $ref: "../../../data-dictionary/commands/stel_aanvullende_vragen.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id
        - actie
  advies_vragen:
    name: Advies vragen
    schema: 
      $ref: "../../../data-dictionary/commands/vraag_advies.yml"
    ui:
      hidden:
        - command_id
        - correlation_id
        - causation_id
        - actie